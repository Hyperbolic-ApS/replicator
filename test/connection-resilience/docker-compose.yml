version: '3.8'

services:
  # Source EventStoreDB (gRPC) - mirrors your production source
  source-esdb:
    container_name: test-source-esdb
    image: kurrentplatform/kurrentdb:25.0
    ports:
      - '2114:2114'
    environment:
      KURRENTDB_INSECURE: 'true'
      KURRENTDB_CLUSTER_SIZE: 1
      KURRENTDB_HTTP_PORT: 2114
      KURRENTDB_RUN_PROJECTIONS: all
      KURRENTDB_ENABLE_ATOM_PUB_OVER_HTTP: 'true'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2114/health/live"\]
      interval: 5s
      timeout: 3s
      retries: 10

  # Sink EventStore (TCP) - mirrors your production sink
  sink-eventstore:
    container_name: test-sink-eventstore
    image: eventstore/eventstore:release-5.0.8
    ports:
      - '2113:2113'
      - '1113:1113'
    environment:
      EVENTSTORE_CLUSTER_SIZE: 1
      EVENTSTORE_EXT_TCP_PORT: 1113
      EVENTSTORE_EXT_HTTP_PORT: 2113
      EVENTSTORE_RUN_PROJECTIONS: all
      EVENTSTORE_START_STANDARD_PROJECTIONS: 'true'
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2113/health/live"\]
      interval: 5s
      timeout: 3s
      retries: 10

  # Mock HTTP Transform Service - can simulate failures
  mock-transform:
    container_name: test-mock-transform
    build:
      context: ./mock-transform
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      PORT: 3000
      FAILURE_MODE: 'none'  # Options: none, unavailable, timeout, intermittent, slow
      FAILURE_RATE: '0.0'   # 0.0 to 1.0 for intermittent failures
      DELAY_MS: '0'         # Delay in milliseconds
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/health"\]
      interval: 5s
      timeout: 3s
      retries: 5

  # Toxiproxy - network chaos for testing connection resilience
  toxiproxy:
    container_name: test-toxiproxy
    image: ghcr.io/shopify/toxiproxy:latest
    ports:
      - '8474:8474'  # Toxiproxy API
      - '21141:21141'  # Proxy for source ESDB
      - '11131:11131'  # Proxy for sink EventStore TCP
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8474/version"\]
      interval: 5s
      timeout: 3s
      retries: 5

  # Toxiproxy configuration
  toxiproxy-config:
    container_name: test-toxiproxy-config
    image: ghcr.io/shopify/toxiproxy:latest
    depends_on:
      toxiproxy:
        condition: service_healthy
      source-esdb:
        condition: service_healthy
      sink-eventstore:
        condition: service_healthy
    entrypoint: /bin/sh
    command:
      - -c
      - |
        set -e
        echo "Configuring Toxiproxy proxies..."
        
        # Create proxy for source ESDB gRPC
        /toxiproxy-cli -h toxiproxy:8474 create source-esdb -l 0.0.0.0:21141 -u source-esdb:2114 || true
        
        # Create proxy for sink EventStore TCP
        /toxiproxy-cli -h toxiproxy:8474 create sink-eventstore -l 0.0.0.0:11131 -u sink-eventstore:1113 || true
        
        echo "Toxiproxy proxies configured successfully"
        tail -f /dev/null

networks:
  default:
    name: test-repl-network
